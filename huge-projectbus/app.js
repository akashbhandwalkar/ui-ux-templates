// const width = 1700;
// const scale = 800 / window.innerHeight;

console.log("scale", {
    height: window.innerHeight,
    width: window.innerWidth
});
// const body = document.getElementsByTagName("body")[0];
// body.style.transform = `scale(${scale})`;